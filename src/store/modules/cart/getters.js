export default {
    products(state) {
        return state.items;
    },
    total(state) {
        return state.total;
    },
    quantity(state) {
        return state.quantity;
    },
    captcha(state) {
        return state.captcha;
    }
};